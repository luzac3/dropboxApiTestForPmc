const path = require('path');

module.exports = {

  mode: 'development',

  devtool: 'inline-source-map',

  devServer: {
      index: "./typescriptTest/entrance.html",
      port: 9223,
      historyApiFallback: true,
  },

  entry: {
    main: './typescriptTest/js/ts/main.ts',
    oAuth: './typescriptTest/js/ts/oAuth.ts',
    entrance: './typescriptTest/js/ts/entrance.ts',
  },

  output: {
        path: __dirname + '/typescriptTest/js/dist',
        filename: '[name].js'
    },

  module: {
    rules: [
      {
        test: /\.ts$/,
        use: 'ts-loader',
      },
      {
        test: /\.json$/,
        loader: 'json-loader',
        type: "javascript/auto"
      },
    ],
  },
  resolve: {
    extensions: [
      '.ts', '.js',
    ],
  },
};
